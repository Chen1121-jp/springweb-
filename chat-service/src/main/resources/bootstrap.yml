spring:
  application:
    name: chat-service
  profiles:
    active: local
  cloud:
    nacos:
      server-addr: ${dm.nacos.host:localhost}:8848
      config:
        file-extension: yaml
#        shared-configs:
#          - dataId: shared-log.yaml
#          - dataId: shared-swagger.yaml
